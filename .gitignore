# ────────────────────────── Python artefacts ──────────────────────────
__pycache__/
*.py[cod]
*.so
*.pyd
*.egg-info/
.eggs/
.build/
dist/
pip-wheel-metadata/

# Poetry / PDM / Hatch caches
.cache/
.pytest_cache/
.coverage
htmlcov/
# Note: poetry.lock is intentionally NOT ignored - it should be committed for reproducible builds

# Local virtual-envs (venv, pipenv, conda envs created inside project)
venv/
.env/
.env.bak/
.venv/
.ipynb_checkpoints/

# ────────────────────────── IDE / Editor files ────────────────────────
# VSCode
.vscode/
# JetBrains (PyCharm, IDEA, etc.)
.idea/
# others
.DS_Store
Thumbs.db

# ────────────────────────── secrets / config ──────────────────────────
# Environment variables & user-specific settings
*.env
.env.*

# Integration test credentials
tests/config/local_credentials.py
integration_config.json
*.key
*.secret
# (keep .env.sample checked-in instead)

# ────────────────────────── Docker build cache ────────────────────────
# If you copy files into the image that shouldn’t reach git
docker-cache/
*/*/target/

# ────────────────────────── Miscellaneous ─────────────────────────────
# Logs & runtime files
*.log
*.pid
*.sqlite3
# pytest junitxml output
junit-*.xml
# Profiling data
*.prof
# mypy & task runner caches
.mypy_cache/
.pyre/
# Mac resource forks
._*

# ────────────────────────── MockX Gateway specific ─────────────────────
# Symbol mapping files
symbol_mappings.json
symbol_mappings_*.json

# Test coverage reports
.coverage
htmlcov/
.coverage.*

# Ruff cache
.ruff_cache/

# Build artifacts
build/
dist/
*.egg-info/
